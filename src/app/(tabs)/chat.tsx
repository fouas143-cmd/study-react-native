import { TabPlaceholder } from "@/components/tab-placeholder";

/** Renders the placeholder for the upcoming AI tutor chat. */
export default function ChatScreen() {
  return (
    <TabPlaceholder title="Chat" subtitle="AI tutor chat lives here soon." />
  );
}
