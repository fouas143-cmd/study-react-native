import { TabPlaceholder } from "@/components/tab-placeholder";

/** Renders the placeholder for the upcoming lesson list. */
export default function LearnScreen() {
  return (
    <TabPlaceholder title="Learn" subtitle="Lessons live here soon." />
  );
}
