# Nota sesi 2026-09-10 — sistem handoff + mercu responsif

## Dibuat sesi ini

1. Skrin onboarding siap penuh: struktur, logo + "duolinggo", tajuk,
   ilustrasi, butang (tanpa dots).
2. Logo kekal `mascot-logo` (fox); ilustrasi utama ditukar ke parrot.
3. `mascot-onboarding.png`: crop parrot + 5 bubbles dari fail Gemini
   pemilik (`ug5u29...jpg`), kemudian key-out checkerboard ke telus
   (flood-fill dari tepi, 328k piksel telus, sah atas latar merah).
4. Layout maskot: tengah menegak (`flex-1` + `justify-center`), turun
   sikit (`pt-8`), saiz sederhana.
5. Responsif orientasi: portrait `700`, landscape `height * 0.35`;
   `app.json` dibuka ke `default`.
6. Audit konflik: `AGENTS.md` ("be concise") lawan format storytelling —
   selesai dengan pengecualian gaya (keputusan pemilik).
7. Sistem continuity pilihan C dipasang (fail-fail sesi ini).

## Keputusan (rujuk ADR)

- ADR-0001: NativeWind untuk styling.
- ADR-0002: imej berpusat dalam `constants/images.ts`.
- ADR-0003: saiz responsif ikut peratus, bukan nombor mati.

## Bukti verify

- `npx tsc --noEmit`: bersih. `npm run lint`: bersih.
- Tiada test dalam repo — langkah test N/A.

## Diserahkan ke sesi seterusnya

- TODO + perangkap dalam `HANDOFF.md` (sumber semasa, baca itu dulu).
- Design belum buat: kad kursus, skrin sign-up (tangguh).
